channels:
    email:
        type: email
        from: 'alerte@exemple.test'
        recipient: 'alice@exemple.test'

    free_mobile:
        type: free_mobile
        # "null" scheme (not "freemobile") to never touch the network in tests: see
        # NullTransportFactory, tagged texter.transport_factory.
        recipient: 'null://null?phone=0600000000'

    mattermost:
        type: mattermost
        # "null" scheme (not "mattermost") to never touch the network in tests: see
        # NullTransportFactory, also tagged chatter.transport_factory.
        recipient: 'null://null?channel=securite'
